schema_version = "1.3"

[task]
name = "swe-bench/sympy-16669"
description = "Fix PythonCodePrinter to support Indexed objects in SymPy"
authors = [{ name = "zackahr", email = "ziko.isezzine@gmail.com" }]
keywords = ["python", "sympy", "printing", "codegen"]

[metadata]
difficulty_explanation = "Small change in printer class"
category = "coding"
tags = ["sympy", "printer", "indexed"]

[environment]
os = "linux"
build_timeout_sec = 600

[agent]
timeout_sec = 300

[verifier]
timeout_sec = 60
command = "bash tests/test.sh"
